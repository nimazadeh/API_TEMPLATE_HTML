# گزارش بازبینی ریسپانسیو و اسکرول

**تاریخ:** ۲۰۲۶-۰۹-۰۸

**نتیجه:** ایراد اسکرول تودرتوی محتوای اصلی اصلاح شد؛ پس‌زمینهٔ دعوت با ورود هماهنگ شد؛ آزمون‌های زیر در مرورگر واقعی Chromium پاس شدند.

## آیا رفتار قبلی مشکل بود؟

بله؛ نسخهٔ قبلی در این بخش ریسپانسیو کامل نبود.

نواری که برای دیدن ادامهٔ محتوا به پایین می‌کشیدید، **اسکرول عمودیِ داخلی** بود، نه افقی. جابه‌جایی سمت نوار با RTL/LTR به‌تنهایی ایراد نیست؛ مشکل این بود که محتوای اصلی به ارتفاع نمایشگر محدود شده و اسکرول جداگانه داشت، درحالی‌که خود سند هم می‌توانست بلندتر از نمایشگر شود.

علاوه بر این، در موبایل **بیرون‌زدگی افقی واقعی** هم وجود داشت: پیش از اصلاح، عرض قابل‌اسکرول داشبورد فارسی در نمایشگر ۳۹۰ پیکسلی حدود **۷۱۴ پیکسل** بود؛ بعد از اصلاح، همان عرض **۳۹۰ پیکسل** است.

### علت‌های اصلی

- `.app-main` دارای `height: calc(100dvh - ...)` و `overflow-y: auto` بود.
- منوی کناری بدون محدودیت مناسب ارتفاع، ارتفاع کل Grid و سند را افزایش می‌داد.
- قانون `display: none` منوی کناری در موبایل با `display: flex` در فایل CSS بعدی بازنویسی می‌شد؛ منو در Grid موبایل فضای اضافه می‌ساخت.
- ستون `1fr` و بعضی Grid/Flexهای داخلی، حداقل عرض محتوای خود را به صفحه تحمیل می‌کردند.
- هدر و برخی کنترل‌ها برای عرض‌های باریک چیدمان جایگزین نداشتند.

## اصلاحات انجام‌شده

| بخش | اصلاح |
| --- | --- |
| داشبورد و تمام صفحات دارای پوستهٔ برنامه | حذف ارتفاع ثابت و اسکرول داخلی محتوای اصلی؛ استفاده از اسکرول طبیعی سند و ستون‌های `minmax(0, 1fr)`، بدون مخفی‌کردن بیرون‌زدگی روی `html` یا `body`. |
| منوی کناری | ثابت‌ماندن در دسترس هنگام اسکرول صفحه؛ فقط فهرست طولانی ناوبری در ارتفاع کم اسکرول می‌شود. کنترل‌های پایین منو از نمایشگر خارج نمی‌شوند. |
| هدر موبایل و تبلت | جستجو به شکل آیکون؛ حفظ دسترسی به زبان، پوسته و اعلان‌ها؛ انتقال انتخاب محیط به منوی موبایل؛ نام دسترس‌پذیر و راهنمای شناور برای آیکون‌های منوی تبلت. |
| تغییر اندازهٔ نمایشگر | هنگام خروج از حالت موبایل، منوی موبایل بسته می‌شود و لایهٔ تاریک و قفل اسکرول آن باقی نمی‌مانند. |
| کارت‌ها و کنترل‌ها | چیدمان مناسب‌تر کارت‌های داشبورد و متریک‌ها؛ جلوگیری از شکستن عددها وسط رقم‌ها؛ امکان رفتن فیلترها، سرآیند کارت‌ها، عنوان نمودار و کنترل‌های جدول به خط بعد. |
| متن‌ها و کدهای طولانی | جلوگیری از بیرون‌زدگی شناسه‌ها و URLها در فید فعالیت و کدهای داخل متن؛ نمونه‌کدهای `pre` همچنان LTR و دارای اسکرول محلی هستند. |
| نوار پایین موبایل | فضای کافی در انتهای محتوای سند، با احتساب safe area، برای اینکه آخرین محتوا زیر نوار ثابت نماند. |
| مستندات و مرجع API | اصلاح بازگشت به ابتدای سند هنگام انتخاب مقاله/نقطهٔ پایانی؛ نگه‌داشتن ناوبری چسبان و مقصد لینک‌ها پایین هدر؛ دسترسی به آخرین گزینهٔ منو در نمایشگر کم‌ارتفاع. |
| پنل‌های جزئیات RTL | اصلاح اولویت CSS موقعیت چپ/راست Bootstrap؛ پنلِ انتهای منطقی اکنون در فارسی سمت چپ و در انگلیسی سمت راست باز می‌شود. |
| صفحات عمومی | اصلاح هدر باریک، در دسترس قرارگرفتن تغییر زبان در موبایل، اندازهٔ تیترها، Grid پیش‌نمایش محصول و لینک‌های فهرست صفحات. |
| پنجرهٔ جستجو | اصلاح عرض ورودی و ارتفاع پنجره در صفحهٔ باریک/کوتاه؛ دکمهٔ بستن قابل لمس، بستن با کلیک بیرون و Escape، گردش Tab داخل پنجره و بازگرداندن فوکوس به کنترل قبلی. |
| دعوت‌نامه | افزودن همان لایه‌های `backdrop--auth`، شبکهٔ خطی و نور پس‌زمینهٔ `login.html`؛ هماهنگی در هر دو پوسته و زبان، بدون پوشاندن یا مسدودکردن فرم. فاصلهٔ لینک‌های پایین فرم نیز اصلاح شد. |

### چه اسکرولی عمداً باقی می‌ماند؟

- **اسکرول عمودی معمول مرورگر** برای صفحه‌ای که محتوایش بلندتر از نمایشگر است؛ ریسپانسیو بودن به معنای جا دادن کل یک صفحهٔ بلند در یک قاب نیست.
- اسکرول محلی **جدول‌های عریض و نمونه‌کدها**، برای حفظ دسترسی به همهٔ ستون‌ها و متن کد.
- اسکرول فهرست ناوبری یا بدنهٔ پنل/پنجره، فقط وقتی محتوای آن از فضای موجود بلندتر است.

محتوای اصلی صفحه دیگر یک قاب اسکرول عمودیِ جدا نیست.

## نتیجهٔ آزمون‌ها

**محیط اجرا:** Playwright و Chromium headless نسخهٔ `149.0.7827.0`، روی خروجی production ساخته‌شده با Vite؛ نه صرفاً بررسی ایستا یا jsdom.

| بررسی | نتیجه |
| --- | --- |
| `npm run build` | موفق؛ خروجی هر ۳۱ صفحه ساخته شد. |
| آزمون‌های موجود ترجمه و عملکرد | **۴۶ از ۴۶ پاس**. |
| آزمون‌های جدید ریسپانسیو و تعامل | **۱۵۱ از ۱۵۱ پاس**. |
| اجرای یکجای `npm test` | **۱۹۷ از ۱۹۷ پاس**. |
| ماتریس چیدمان | **۱۱۱۶ بررسی موفق**: ۳۱ صفحه × ۲ زبان × ۲ پوسته × ۹ عرض. |
| خطای JavaScript در ماتریس چیدمان | موردی ثبت نشد. |
| بیرون‌زدگی افقی سند و اسکرول داخلی محتوای اصلی | در ماتریس آزمون موردی ثبت نشد. |
| `git diff --check` | موفق. |

عرض‌های آزمون دائمی: **۳۲۰، ۳۶۰، ۳۹۰، ۵۷۶، ۷۶۸، ۹۹۲، ۱۲۰۰، ۱۴۴۰ و ۱۹۲۰ پیکسل**. حالت روشن همچنین بنر محیط آزمایشی را بررسی می‌کند.

آزمون‌های تعاملی شامل اسکرول با چرخ ماوس، رسیدن به آخرین کارت، هدر چسبان، منو و تغییر زبان/پوسته در موبایل، تغییر اندازه با منوی باز، اسکرول محلی جدول، جهت پنل جزئیات، ناوبری مستندات، فرم دعوت در ارتفاع کم، یکسان‌بودن پس‌زمینهٔ صفحات ورود/دعوت/بازیابی و نمایش محتوا با انیمیشن فعال هستند.

### بازاجرای آزمون‌ها

```bash
npm ci
npx playwright install chromium
npm test

# فقط آزمون‌های ریسپانسیو
npm run test:responsive

# فقط آزمون‌های ترجمه و رفتارهای موجود
npm run test:i18n
```

آزمون‌ها خروجی production را می‌سازند و با `vite preview` اجرا می‌کنند. برای مرورگر نصب‌شدهٔ سازمانی/سیستمی، تنظیم اختیاری `CHROMIUM_EXECUTABLE_PATH` در پیکربندی موجود پشتیبانی می‌شود.

## حدود این بازبینی

- این نتیجه تأیید **محدودهٔ آزمون‌شده** است، نه ادعای «بی‌نقص روی تمام مرورگرها و دستگاه‌ها».
- Safari و Firefox، موبایل فیزیکی، صفحه‌کلید واقعی iOS/Android و safe area واقعی دستگاه در این اجرا بررسی نشده‌اند؛ ارتفاع کم شبیه‌سازی شده است.
- ممیزی کامل WCAG، کنتراست و عملکرد/Lighthouse موضوع این بازبینی نبود.
- قالب همچنان نمونهٔ HTML با داده و احراز هویت شبیه‌سازی‌شده است؛ این اصلاحات آن را به بک‌اند یا سامانهٔ احراز هویت واقعی متصل نمی‌کند.

**فایل آزمون دائمی:** `tests/responsive.spec.js`

**قرارداد چیدمان:** `src/scss/layouts/_app-shell.scss` و بخش Responsive layout در `README.md`.
